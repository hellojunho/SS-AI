name: ss_ai_mobile
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
